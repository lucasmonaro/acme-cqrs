server:
    port: 8080
spring:
    datasource:
        url: jdbc:mysql://localhost:3306/acme_cqrs?useSSL=false&allowPublicKeyRetrieval=true
        username: root
        password: root
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        hibernate:
            ddl-auto: update
    kafka:
        bootstrap-servers: localhost:9092
        consumer:
            group-id: test-consumer-group


